# Group Contribution Details

**Module:** CIT300 – Data Structures and Algorithms
**Assignment:** Graded Practical Assignment 1 (Week 10)

| Name | Student ID | Assigned Responsibility | Individual Contribution |
|---|---|---|---|
| RM. Almunthazeer | 23DA2-1148 | Linked list implementation and student-record management | Implemented `StudentNode`, `StudentLinkedList` (add/update/delete/search/display), and the student-record menu operations. |
| ABF. Sarafa | 23DA2-1167 | Stack and queue implementation and related operations | Implemented `Action`, `ActionStack` (recent actions/history), `ServiceRequest`, and `ServiceQueue` (service request handling). |
| RF. Riska | 23DA2-1137 | AVL tree implementation and hashing/search functionality | Implemented `AVLNode`, `StudentAVLTree` (insert/delete/search with rotations), and `StudentHashTable` (separate chaining). |
| JF. Sharafa | 23DA2-1117 | Graph implementation, campus locations, connections, and BFS/DFS traversal | Implemented `CampusGraph` (adjacency list, add/remove location/connection) and BFS/DFS traversal logic. |

**All members:** Integration of components into `ConsoleMenu`, input validation
(`InputValidator`), testing, debugging, README/documentation, and GitHub
collaboration (branches, commits, pull requests).

_Fill in specific commit links / dates here before final submission._
